import { h } from "preact";

const CVSection = () =>  {
  return (
    <section className={css["cv-section"]} >cv_section</section>
  )
}

export default CVSection;

css`
  .cv-section {
    color: red;
  }
`
